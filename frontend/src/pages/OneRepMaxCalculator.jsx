import React, { useMemo, useState } from "react";

export default function OneRepMaxCalculator() {
  const [weight, setWeight] = useState(80);
  const [reps, setReps] = useState(5);
  const result = useMemo(() => { const w=Number(weight)||0, r=Number(reps)||0; return r>0 ? w*(1+r/30) : 0; }, [weight,reps]);
  return <main className="mx-auto max-w-4xl px-4 py-10"><h1 className="text-4xl font-bold">One Rep Max Calculator</h1><p className="mt-3 text-lg">Estimate your one-repetition maximum (1RM) from a weight and number of repetitions.</p><section className="mt-8 rounded-2xl border p-6 grid gap-5 sm:grid-cols-2"><label>Weight lifted<input className="mt-2 w-full rounded-lg border p-3" type="number" min="0" value={weight} onChange={e=>setWeight(e.target.value)}/></label><label>Repetitions<input className="mt-2 w-full rounded-lg border p-3" type="number" min="1" max="30" value={reps} onChange={e=>setReps(e.target.value)}/></label><div className="sm:col-span-2 rounded-xl border p-5"><b>Estimated 1RM</b><div className="text-3xl font-semibold">{result.toFixed(1)} kg</div></div></section><article className="prose mt-10 max-w-none"><h2>How the One Rep Max Calculator Works</h2><p>This calculator estimates the maximum weight you could theoretically lift once using the Epley equation.</p><h2>1RM Formula</h2><p>Estimated 1RM = weight × (1 + repetitions ÷ 30).</p><h2>How to Use a 1RM Estimate</h2><p>Use the estimate to help structure strength-training loads. It should not be treated as a guarantee of actual performance.</p><h2>Safety and Limitations</h2><p>Do not attempt a maximal lift solely because a formula predicts it. Technique, fatigue, equipment and training experience affect real performance.</p><h2>Frequently Asked Questions</h2><h3>Is the result exact?</h3><p>No. Repetition equations estimate strength from submaximal performance.</p><h3>Can I use this for every exercise?</h3><p>The equation can provide an estimate for many resistance exercises, but accuracy varies by exercise and repetition range.</p></article></main>;
}
