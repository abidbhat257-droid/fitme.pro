import React, { useMemo, useState } from "react";
export default function PaceCalculator(){const [distance,setDistance]=useState(5),[minutes,setMinutes]=useState(30);const pace=useMemo(()=>{const d=Number(distance)||0,m=Number(minutes)||0;return d>0?m/d:0},[distance,minutes]);const speed=pace>0?60/pace:0;return <main className="mx-auto max-w-4xl px-4 py-10"><h1 className="text-4xl font-bold">Pace Calculator</h1><p className="mt-3 text-lg">Calculate running or walking pace and average speed from distance and time.</p><section className="mt-8 rounded-2xl border p-6"><div className="grid gap-5 sm:grid-cols-2"><label>Distance (km)<input className="mt-2 w-full rounded-lg border p-3" type="number" min="0" step="0.01" value={distance} onChange={e=>setDistance(e.target.value)}/></label><label>Time (minutes)<input className="mt-2 w-full rounded-lg border p-3" type="number" min="0" step="0.1" value={minutes} onChange={e=>setMinutes(e.target.value)}/></label></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-xl border p-5"><span>Average pace</span><div className="text-3xl font-bold">{pace.toFixed(2)} min/km</div></div><div className="rounded-xl border p-5"><span>Average speed</span><div className="text-3xl font-bold">{speed.toFixed(2)} km/h</div></div></div></section><article className="prose mt-10 max-w-none"><h2>What Is Running Pace?</h2><p>Pace describes how much time it takes to cover a unit of distance. A lower minutes-per-kilometre value means a faster pace.</p><h2>Pace Formula</h2><p>Average pace = total time ÷ distance. When time is entered in minutes and distance in kilometres, the result is minutes per kilometre.</p><h2>Using a Pace Calculator</h2><p>Use pace to plan workouts, compare performances over different distances, and estimate finishing times. Terrain, elevation, weather, and fatigue can affect real-world performance.</p><h2>Pace and Fitness Planning</h2><p>Pace works well alongside heart-rate and calorie estimates, but it should not be used as the only measure of fitness. Track how pace changes under comparable conditions.</p><h2>Frequently Asked Questions</h2><h3>What does a faster pace mean?</h3><p>A faster pace means fewer minutes are required to cover the same distance.</p><h3>Can I calculate pace for walking?</h3><p>Yes. The same time-and-distance formula applies to walking, running, cycling, or any activity where distance and elapsed time are known.</p><h3>How accurate is the result?</h3><p>The arithmetic is exact for the supplied inputs; the practical usefulness depends on accurate distance and time measurements.</p></article></main>}
