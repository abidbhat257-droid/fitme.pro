import React, { useMemo, useState } from "react";
export default function CaloriesBurnedCalculator(){const [weight,setWeight]=useState(70),[minutes,setMinutes]=useState(30),[met,setMet]=useState(5);const calories=useMemo(()=>Math.max(0,Number(weight)||0)*Math.max(0,Number(minutes)||0)*Math.max(0,Number(met)||0)/200,[weight,minutes,met]);return <main className="mx-auto max-w-4xl px-4 py-10"><h1 className="text-4xl font-bold">Calories Burned Calculator</h1><p className="mt-3 text-lg">Estimate calories burned during an activity using body weight, duration, and MET intensity.</p><section className="mt-8 rounded-2xl border p-6"><div className="grid gap-5 sm:grid-cols-3"><label>Weight (kg)<input className="mt-2 w-full rounded-lg border p-3" type="number" min="0" value={weight} onChange={e=>setWeight(e.target.value)}/></label><label>Duration (minutes)<input className="mt-2 w-full rounded-lg border p-3" type="number" min="0" value={minutes} onChange={e=>setMinutes(e.target.value)}/></label><label>Activity MET<input className="mt-2 w-full rounded-lg border p-3" type="number" min="0" step="0.1" value={met} onChange={e=>setMet(e.target.value)}/></label></div><div className="mt-6 rounded-xl border p-5"><span>Estimated calories burned</span><div className="text-3xl font-bold">{calories.toFixed(0)} kcal</div></div></section><article className="prose mt-10 max-w-none"><h2>How Calories Burned Is Estimated</h2><p>This calculator uses a MET-based estimate. MET represents the energy cost of an activity relative to resting metabolism. The result changes with body weight, activity duration, and intensity.</p><h2>Formula</h2><p>A common simplified estimate is calories burned = MET × body weight in kilograms × time in minutes ÷ 200.</p><h2>Why the Result Is an Estimate</h2><p>Real energy expenditure varies with fitness, movement efficiency, terrain, temperature, equipment, and individual physiology. Do not treat the displayed number as an exact measurement.</p><h2>Using the Result</h2><p>For weight management, compare estimated activity expenditure with your overall energy intake and maintenance needs. Trends are more useful than assuming every workout burns an exact number of calories.</p><h2>Frequently Asked Questions</h2><h3>What is a MET?</h3><p>A MET is a standardized way to describe the intensity of physical activities relative to resting energy expenditure.</p><h3>Does heavier body weight change calories burned?</h3><p>For the same MET and duration, the estimate increases as body weight increases.</p><h3>Can a fitness tracker give a different result?</h3><p>Yes. Devices use their own models and sensor data, so estimates can differ from MET calculations.</p></article></main>}
